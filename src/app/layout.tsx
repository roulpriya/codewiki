import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["400", "500"],
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
  weight: ["400"],
});

export const metadata: Metadata = {
  title: "Codewiki — repository knowledge, kept current",
  description: "A living, cited wiki for your codebase.",
  openGraph: {
    title: "Codewiki — repository knowledge, kept current",
    description: "Your repository, explained.",
    images: ["/og.png"],
  },
  twitter: {
    card: "summary_large_image",
    title: "Codewiki — repository knowledge, kept current",
    description: "Your repository, explained.",
    images: ["/og.png"],
  },
  icons: {
    icon: "/favicon.svg",
    shortcut: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${jetbrainsMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
